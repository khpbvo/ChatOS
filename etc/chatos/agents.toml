[models]
default = "sonnet"

[agent.orchestrator]
model = "sonnet"
description = "Main routing agent — interprets user intent and delegates to specialists"

[agent.system]
model = "sonnet"
description = "Machine health, maintenance, updates, services, network status, performance"

[agent.files]
model = "sonnet"
description = "File management — browse, create, organize, search, upload/download"

[agent.web]
model = "sonnet"
description = "Web browsing, search, URL fetching, page summaries, iframe handoff"

[agent.media]
model = "sonnet"
description = "Images, video previews, audio playback, media file handling"

[agent.mail]
model = "sonnet"
description = "Email — read inbox, compose, send, search (via IMAP/SMTP MCP)"
